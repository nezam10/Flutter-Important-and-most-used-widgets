import 'package:flutter/material.dart';

class MapTest1View extends StatelessWidget {
  const MapTest1View({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Map Test1 View"),
      ),
    );
  }
}
